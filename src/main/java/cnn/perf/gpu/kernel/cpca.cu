extern "C"
__global__ void weights_gradients(float *x, float *w, float *y)
{
    // TODO
    // int i = blockIdx.x * blockDim.x + threadIdx.x;
    // if (i<n)
    // {
    //     sum[i] = a[i] + b[i];
    // }
}
